# Vault integration

This directory documents how to integrate HashiCorp Vault for storing secrets (Kafka creds, MinIO keys, Triton TLS certs).

In production, create policies and use Kubernetes Vault Injector or init containers to provision secrets.
